-r requirements.txt

pytest==9.1.1
respx==0.23.1
pytest-asyncio==1.3.0

# Local GCS emulator, so the whole stack can be run end to end without a
# cloud account or credentials. Not needed by the test suite, which uses
# InMemoryStorage and fake clients.
gcp-storage-emulator==2026.7.19
